-
  const content = `# IDENTITY.md
  
  ${agent_id ? `- ID: ${agent_id}\n` : ""}- Name: ${agent_name}
  ${icon_id ? `- Icon ID: ${icon_id}\n` : ""}${creature ? `- Creature: ${creature}\n` : ""}`;
!= content
